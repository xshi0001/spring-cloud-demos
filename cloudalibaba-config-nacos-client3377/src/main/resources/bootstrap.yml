server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.130.237:8848 #服务注册中心地址
      config:
        server-addr: 192.168.130.237:8848 #配置中心地址 spring.cloud.nacos.config.server-addr
        file-extension: yaml #指定yaml格式的配置
        namespace: 915019ba-b0a9-40e7-8813-27823fc7e4f4
        group: TEST_GROUP
#nacos-config-client-dev.yaml

#${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}
